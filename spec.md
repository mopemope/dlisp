# 言語仕様 (Language Specification)

このドキュメントでは、`dlisp` プロジェクトの言語仕様について記述します。

## データ型 (Data Types)

- **Integer**: 64ビット整数 (`i64`)。`0` は偽として扱われます。
- **Float**: 64ビット浮動小数点数 (`f64`)。
- **Symbol**: 識別子。
- **List**: 連結リスト。`()` または `nil` は空リストであり、偽として扱われます。
- **Vector**: 可変長配列。`[]` で表記されます。
- **Map**: ハッシュマップ。`{}` で表記されます。
- **String**: 文字列。
- **Bool**: 真偽値（内部的に使用）。`true` / `false`。
- **Nil**: 空リストまたは偽。

※ 条件式において、`nil` と `0` は **偽 (false)** として扱われ、それ以外は **真 (true)** として扱われます。

## スペシャルフォーム (Special Forms)

特殊形式は標準の評価ルールとは異なる評価規則を持つ式です。

### defun
関数を定義します。

**構文:** `(defun <name> (<params>...) <body>...)`

- `<name>`: 関数名（シンボル）。
- `<params>`: 引数リスト（シンボルのリスト）。
- `<body>`: 関数の本体。順に評価され、最後の式の結果が返されます。

**例:**
```lisp
(defun add (a b)
  (+ a b))
```

### defvar
グローバル変数を定義します。変数が未定義の場合のみ値を設定します（既に値がある場合は上書きしません）。

**構文:** `(defvar <name> [<init-value> [<doc-string>]])`

- `<name>`: 変数名（シンボル）。
- `<init-value>`: 初期値（省略可能）。
- `<doc-string>`: ドキュメント文字列（省略可能、現在は無視されます）。

**例:**
```lisp
(defvar *count* 0)
```

### setq
既存の変数の値を更新します。

**構文:** `(setq <name> <value>)`

- `<name>`: 変数名（シンボル）。
- `<value>`: 新しい値。

**例:**
```lisp
(setq *count* (+ *count* 1))
```

### if
条件分岐を行います。

**構文:** `(if <condition> <then> [<else>])`

- `<condition>`: 条件式。`nil` または `0` であれば偽、それ以外は真。
- `<then>`: 真の場合に評価される式。
- `<else>`: 偽の場合に評価される式（省略可能。省略時は `nil` を返す）。

**例:**
```lisp
(if (> x 0)
  (print "Positive")
  (print "Non-positive"))
```

### let
局所変数を定義します（並列バインディング）。

**構文:** `(let ((<var> <val>) ...) <body>...)`

- `((<var> <val>) ...)`: 変数と値のペアのリスト。値は現在の環境で評価されます。
- `<body>`: 変数が束縛された新しい環境で評価される式。

**例:**
```lisp
(let ((x 1) (y 2))
  (print (+ x y)))
```

### lambda
無名関数を作成します。定義時の環境をキャプチャします（クロージャ）。

**構文:** `(lambda (<params>...) <body>...)`

**例:**
```lisp
(map (lambda (x) (* x 2)) '(1 2 3))
```

### quote
引数を評価せずにそのまま返します。`'` (シングルクォート) はこの形式の糖衣構文です。

**構文:** `(quote <expr>)`

**例:**
```lisp
(quote (1 2 3)) ; => (1 2 3)
'(1 2 3)        ; => (1 2 3)
```

### spawn
新しいタスク（軽量スレッド/非同期タスク）を生成して関数を実行します。

**構文:** `(spawn <function-call>)`

- `<function-call>`: 評価結果が関数となる式、または関数呼び出し。

**例:**
```lisp
(spawn (lambda () (print "Async Hello")))
```

### progn
複数の式を順に評価し、最後の式の値を返します。`do` はエイリアスです。

**構文:** `(progn <expr>...)`

**例:**
```lisp
(progn (print "a") (print "b") 42) ;=> 42
```

### cond
多分岐の条件式です。各節のテスト式を順に評価し、最初に真となった節の本体を実行します。

**構文:** `(cond (<test> <body>...) ...)`

- `<test>`: 条件式。真なら対応する `<body>` が評価されます。
- `<body>`: テストが真の場合に順に評価される式。最後の式の値が返されます。

**例:**
```lisp
(cond
  ((= x 1) "one")
  ((= x 2) "two")
  (1       "other"))
```

### and
短絡評価つきの論理積です。引数を左から順に評価し、最初に偽となった値を返します。全て真の場合は最後の値を返します。引数なしは `1` (真) を返します。

**構文:** `(and <expr>...)`

**例:**
```lisp
(and (> x 0) (< x 10)) ;=> 真なら最後の値、偽なら偽の値
```

### or
短絡評価つきの論理和です。引数を左から順に評価し、最初に真となった値を返します。全て偽の場合は最後の値を返します。引数なしは `nil` を返します。

**構文:** `(or <expr>...)`

**例:**
```lisp
(or nil 0 42) ;=> 42
```

## 使用できる関数 (Available Functions)

標準で定義されている組み込み関数です。

### 算術演算 (Arithmetic)

- **`(+ <args>...)`**: 数値の加算。整数と浮動小数点数が混在可能（結果は浮動小数点数になる）。
- **`(- <arg1> [<args>...])`**: 減算。引数が1つの場合は符号反転。
- **`(* <args>...)`**: 乗算。
- **`(/ <arg1> <arg2> ...)`**: 除算。整数同士は整数除算、浮動小数点数が混在すると浮動小数点数。3引数以上で連鎖除算。
- **`(% <arg1> <arg2>)`** / **`(mod <arg1> <arg2>)`**: 剰余演算。

### 比較演算 (Comparison)

- **`(= <arg1> <arg2>)`**: 等価比較。真なら `1`、偽なら `0` を返します。
- **`(> <arg1> <arg2>)`**: 大なり比較。数値のみ。
- **`(< <arg1> <arg2>)`**: 小なり比較。数値のみ。
- **`(>= <arg1> <arg2>)`**: 以上比較。数値のみ。
- **`(<= <arg1> <arg2>)`**: 以下比較。数値のみ。
- **`(/= <arg1> <arg2>)`**: 非等価比較。等しくなければ `1`、等しければ `0`。

### 論理演算 (Logical)

- **`(not <expr>)`**: 論理否定。`nil` または `0` なら `1` (真)、それ以外なら `nil` (偽) を返します。

### 入出力 (I/O)

- **`(print <args>...)`**: 引数をスペース区切りで標準出力に表示し、改行します。戻り値は `nil`。
- **`(read-file <path>)`**: 指定されたパスのファイルを読み込み、内容を文字列として返します。エラー時は `nil`。

### 制御・ユーティリティ (Control & Utility)

- **`(sleep <ms>)`**: 指定されたミリ秒数だけスリープします。

### 文字列操作 (String Operations)

- **`(str <args>...)`**: 引数を文字列に変換して連結します。
- **`(string-length <str>)`**: 文字列の文字数を返します。
- **`(substring <str> <start> <end>)`**: 部分文字列を返します（`start` から `end` の手前まで、0起算）。
- **`(string-append <str>...)`**: 文字列引数のみを連結します。

### 型判定 (Type Predicates)

- **`(nil? <x>)`**: `nil` なら `1`、それ以外は `nil`。
- **`(list? <x>)`**: リストまたは `nil` なら `1`。
- **`(number? <x>)`**: 整数または浮動小数点数なら `1`。
- **`(string? <x>)`**: 文字列なら `1`。
- **`(symbol? <x>)`**: シンボルなら `1`。
- **`(vector? <x>)`**: ベクタなら `1`。
- **`(map? <x>)`**: マップなら `1`。
- **`(type-of <x>)`**: 型名を文字列で返します（`"integer"`, `"float"`, `"string"`, `"symbol"`, `"list"`, `"vector"`, `"map"`, `"nil"`, `"function"`, `"macro"`）。


### コレクション操作 (Collection Operations)

#### List
- **`(list <args>...)`**: 引数を要素とするリストを作成します。
- **`(car <list>)`**: リストの最初の要素を返します。空リストまたは `nil` は `nil`。エイリアス: `first`。
- **`(cdr <list>)`**: リストの最初の要素を除いた残りを返します。要素が1つ以下なら `nil`。エイリアス: `rest`。
- **`(cons <elem> <list>)`**: 要素をリストの先頭に追加した新しいリストを返します。第2引数が `nil` の場合は単一要素リストを作成します。
- **`(count <col>)`**: リストまたはベクタの長さを返します。`nil` は `0`。
- **`(nth <col> <index>)`**: リストまたはベクタの指定インデックスの要素を返します。範囲外は `nil`。
- **`(conj <col> <items>...)`**: コレクションに要素を追加します。
    - リストの場合：先頭に追加されます（逆順）。
    - ベクタの場合：末尾に追加されます。
    - `nil` の場合：リストとして扱われ、先頭に追加されます。

#### Vector
- **`(vector <args>...)`**: 引数を要素とするベクタを作成します。
- **`(get <vector> <index> [<default>])`**: ベクタのインデックス要素を取得します。範囲外の場合は `default` (省略時は `nil`) を返します。
- **`(contains? <vector> <index>)`**: ベクタに指定インデックスが存在するか確認します。

#### Map
- **`(hash-map <key> <val> ...)`**: キーと値のペアからハッシュマップを作成します。引数は偶数個である必要があります。
- **`(get <map> <key> [<default>])`**: マップからキーに対応する値を取得します。キーがない場合は `default` (省略時は `nil`) を返します。
- **`(assoc <map> <key> <val> ...)`**: マップに新しいキーと値のペアを追加/更新した新しいマップを返します。
- **`(dissoc <map> <key> ...)`**: マップから指定されたキーを削除した新しいマップを返します。
- **`(keys <map>)`**: マップのキーのリストを返します。
- **`(vals <map>)`**: マップの値のリストを返します。
- **`(contains? <map> <key>)`**: マップに指定キーが存在するか確認します。

### 高階関数 (Higher-Order Functions)

- **`(map <func> <list>)`**: リストの各要素に関数を適用した結果のリストを返します。
- **`(filter <func> <list>)`**: リストの各要素に関数を適用し、真となる要素のみのリストを返します。
- **`(reduce <func> <init> <list>)`**: 初期値とリストの各要素に関数を順次適用し、単一の値を返します。
